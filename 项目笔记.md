# P-项目 | Image Background Remover

> AI图片背景移除网站 - 训练营实战项目

---

## 基本信息

| 字段 | 内容 |
|------|------|
| **项目名** | Image Background Remover |
| **状态** | 🚧 进行中 |
| **Owner** | 先生 + 贾维斯 |
| **创建日期** | 2026-03-26 |
| **类型** | AI工具网站 |

---

## 项目目标

- 练手项目（AI编程训练营）
- 目标：真正可用的产品

## 技术方案

| 组件 | 方案 |
|------|------|
| 前端 | React + Vite + Tailwind CSS |
| 后端 | Cloudflare Worker (API代理) |
| AI服务 | Remove.bg API |
| 部署 | Cloudflare Pages |
| 免费额度 | 50次/月 |

## 文件结构

```
P-项目/image-bg-remover/
├── src/                    # React前端
│   ├── App.jsx            # 主组件
│   ├── main.jsx           # 入口
│   ├── index.css          # 全局样式
│   ├── components/        # UI组件
│   │   ├── DropZone.jsx  # 上传组件
│   │   └── Result.jsx    # 结果展示
│   └── lib/
│       └── api.js        # API调用
├── functions/             # Cloudflare Worker
│   └── [[catchall]].js   # API代理
├── dist/                  # 构建产物
├── package.json
├── vite.config.js
├── tailwind.config.js
├── wrangler.toml
└── README.md
```

---

## 当前进度

### ✅ 已完成
- [x] 需求分析 + 设计方案
- [x] 项目初始化
- [x] 前端开发（React + Tailwind）
- [x] Cloudflare Worker API代理
- [x] 构建通过

### ⏳ 待完成（先生晚上继续）
- [ ] 注册 Remove.bg API 获取 Key
- [ ] 配置 Cloudflare Worker 环境变量
- [ ] 部署到 Cloudflare Pages
- [ ] 测试完整流程

### 📋 下一步（先生今晚执行）
1. 获取 Remove.bg API Key（https://www.remove.bg/api）
2. 运行 `npx wrangler pages deploy dist` 部署
3. 在Cloudflare Dashboard配置 `REMOVE_BG_API_KEY`
4. 测试上传图片
1. 获取 Remove.bg API Key
2. 运行 `npx wrangler pages deploy dist`
3. 配置环境变量 `REMOVE_BG_API_KEY`
4. 测试上传图片

---

## 待解答问题

- [ ] 目标用户定位（还没想清楚）
- [ ] Remove.bg超出50次后的定价策略

---

## 相关资料

- Remove.bg API: https://www.remove.bg/api
- Cloudflare Pages: https://pages.cloudflare.com/
- 部署命令: `npx wrangler pages deploy dist`

---

*最后更新：2026-03-26 13:18*
